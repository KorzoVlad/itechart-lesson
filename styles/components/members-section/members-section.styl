@import "../../variables.styl";

.members-section {
    padding: 60px 0;

    &__list {
        flex-row();
        margin-top: 30px;

        &:first-child {
            margin-top: 0;
        }
    }
}